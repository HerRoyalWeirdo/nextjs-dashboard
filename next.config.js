/** @type {import('next').NextConfig} */

const nextConfig = {
    // experimental: {
    //     ppr: 'incremental',
    // },
    //only for the latest canary version of next js
};

module.exports = nextConfig;
